import { createTheme } from "@mui/material";

const customTheme=createTheme({
    palette:{
        mode:"light",
        primary:{
            main:"#00927C"
        },
        secondary:{
            main:"#EAF0F1"
        }
    }
})

export default customTheme;